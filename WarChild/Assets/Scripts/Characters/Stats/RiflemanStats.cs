using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RiflemanStats : CharacterStats
{
    // Start is called before the first frame update
    public RiflemanStats()
    {
        maxHealth = 100f;
        movSpeed = 0.5f;
        armour = 1.4f;
    }
}
